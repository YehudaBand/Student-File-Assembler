[project]
name = "student-file-assembler"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
    "flask",
    "gunicorn",
    "python-dotenv",
    "google-api-python-client",
    "google-auth-httplib2",
    "google-auth-oauthlib",
    "pypdf",
    "pikepdf",
    "reportlab",
    "Pillow",
]

[tool.vercel]
entrypoint = "api.index:app"
